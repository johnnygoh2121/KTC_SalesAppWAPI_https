


use KTCW_COMMON
go 

create procedure sp_SelectPafChargeCodeLine 
@webDb as nvarchar(100), 
@docEntry as int
as
declare @sql as nvarchar(max)
select @sql = '

SELECT * 
FROM ['+@webDb+'].[dbo].[PAF1] 
WHERE DocEntry =' + QUOTENAME(@docEntry, '''')

--print @sql
exec sp_executesql @sql

