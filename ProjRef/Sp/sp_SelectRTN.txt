USE [KTCW_COMMON]
GO
/****** Object:  StoredProcedure [dbo].[sp_SelectRTN]    Script Date: 18-Jun-21 17:18:50 ******/
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

ALTER procedure [dbo].[sp_SelectRTN]
@webDb as nvarchar(100), 
@startDate as datetime, 
@endDate as datetime, 
@subsi as nvarchar(100)

as 
declare @sql as nvarchar (max) = '

select 
	' +@subsi+ ' [Subsi]
	,* from ['+@webDb+'].[dbo].[RTN]
where convert(date, DOCDATE) >= '+ quotename (@startDate, '''')+'
and convert(date, DOCDATE) <= '+ quotename (@endDate, '''') 

--print @sql
exec sp_executesql @sql